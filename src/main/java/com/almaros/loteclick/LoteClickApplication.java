package com.almaros.loteclick;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Clase principal de inicio para la aplicación LoteClick.
 * Inicializa el contexto de Spring Boot.
 */
@SpringBootApplication
public class LoteClickApplication {

    public static void main(String[] args) {
        SpringApplication.run(LoteClickApplication.class, args);
    }
}
